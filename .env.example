# Supabase Configuration
# Get these from your Supabase project: Settings > API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# CoinGecko API Configuration (Optional)
# Get a free API key from: https://www.coingecko.com/en/api/pricing
# Free tier: 10,000 calls/month (vs 10-30 calls/min without key)
VITE_COINGECKO_API_KEY=your-coingecko-api-key-here

# Note: These are prefixed with VITE_ so Vite exposes them to the client
# The anon key is safe to expose - it only has public permissions
